# Generated / vendored — never format these
pnpm-lock.yaml
**/dist/
**/.next/
**/.turbo/
**/.astro/
**/coverage/
**/CHANGELOG.md
# Regenerated by `next dev`/`next build` with its own quote style, which would
# otherwise fail format:check for anyone who ran the app before committing.
**/next-env.d.ts
# Local agent/editor state is not a project artifact and may contain
# machine-specific JSON formatting.
.claude/settings.local.json
